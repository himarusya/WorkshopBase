@model IEnumerable<WorshopBase.Models.User>
@{
    ViewBag.Title = "Список пользователей-администраторов";
}

<a asp-action="CreateAdmin">Добавить пользователя-администратора</a><br />
<a asp-action="Index">Назад</a>

<table class="table">
    <tr><th>Email</th><th></th></tr>
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                <form asp-action="Delete" asp-route-id="@user.Id" method="post">
                    <a class="btn btn-sm btn-primary" asp-action="ChangePassword" asp-route-id="@user.Id">Сменить пароль</a>
                    <button type="submit" class="btn btn-sm btn-danger">
                        Удалить
                    </button>
                </form>
            </td>
        </tr>
    }
</table>